# This file contains secrets and should NEVER be committed to Git.
# Copy this to secrets.env and fill in your actual values.

# --- Tailscale ---
# Generate an auth key from your Tailscale admin console (e.g., reusable, ephemeral)
TAILSCALE_AUTH_KEY=tskey-auth-XXXXXXXXXXXXXXXX

# --- Domain & DNS ---
# Your domain name for public access
DOMAIN=yourdomain.com
# Email for Let's Encrypt certificates
ACME_EMAIL=your-email@example.com
# Cloudflare API token (for DNS-01 challenge)
CF_DNS_API_TOKEN=your-cloudflare-api-token
# RPi-Home Tailscale IP (for Traefik backend routing)
RPI_HOME_IP=100.88.245.35

# --- Cloud VM Services (Authentik) ---
# Generate with: openssl rand -base64 32
AUTHENTIK_SECRET_KEY=your-authentik-secret-key-here
AUTHENTIK_DB_PASSWORD=your-authentik-db-password

# --- Backups (Rclone) ---
# Backblaze B2 Credentials (use with rclone)
B2_KEY_ID=your-b2-key-id
B2_APP_KEY=your-b2-app-key
B2_BUCKET=your-unique-b2-bucket-name

# Linode Object Storage Credentials (Optional Mirror)
LINODE_ACCESS_KEY_ID=your-linode-key-id
LINODE_SECRET_ACCESS_KEY=your-linode-secret-key
LINODE_BUCKET=your-unique-linode-bucket-name
LINODE_CLUSTER=your-linode-cluster-e-g-us-east-1

# --- Databases ---
# Strong password for the main Postgres user
PG_PASSWORD=your-strong-postgres-password

# --- Application Database Passwords ---
# Individual database passwords for each app
MEALIE_DB_PASSWORD=your-mealie-db-password
WALLOS_DB_PASSWORD=your-wallos-db-password
PAPERLESS_DB_PASSWORD=your-paperless-db-password

# --- Application Secret Keys ---
# Homarr encryption key - Generate with: openssl rand -hex 32
HOMARR_SECRET_KEY=your-homarr-secret-key-here
# Paperless-ngx secret key - Generate with: openssl rand -base64 64
PAPERLESS_SECRET_KEY=your-paperless-secret-key-here

# --- Application Admin Passwords ---
# Password for VS Code Server web UI
CODE_PASSWORD=your-strong-code-server-password
CODE_SUDO_PASSWORD=your-code-sudo-password
# AdGuard Home admin credentials (Cloud VM)
ADGUARD_USERNAME=admin
ADGUARD_PASSWORD=your-secure-adguard-password
# Application admin passwords
MEALIE_DEFAULT_PASSWORD=your-mealie-password
WALLOS_ADMIN_PASSWORD=your-wallos-password
# OpenAI API key for Open WebUI (optional)
OPENAI_API_KEY=your-openai-api-key
# Ollama base URL (optional - leave empty if not using local Ollama)
OLLAMA_BASE_URL=
